<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@picture.image.url) if @picture.image && @picture.image.url %>
</p>

<p>
  <strong>Content:</strong>
  <%= @picture.content %>
</p>

<% if @picture == current_user %>
  <%= link_to 'Edit', edit_picture_path(@user) %>
<% end %>

<%= link_to 'Back', pictures_path %>
